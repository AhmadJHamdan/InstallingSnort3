# Remember Always to take a snapshot for your machine before any upgrade or new installation
# Go to root mode
# Download snort3_installation_script.
# run the below command in the directory where the script is installed:
chmod +x snort3_installation_script

# then run the script
./snort3_installation_script

# take snapshot of the VM with working Snort3 if you wish
